[tool.poetry]
name = "asyncrai"
version = "0.2.0"
description = ""
authors = ["_Map_ <blendfan@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
